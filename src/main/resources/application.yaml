spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/practicespringboot
    username: Vichet
    password: 100106
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: vichetchoub844@gmail.com
    password: jomd rzvd vowi sbfe
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
